# Runtime local

Ce dossier rassemble les fichiers produits localement par l'application.

- `mysql-data/` : donnees de la base MySQL ou MariaDB locale

Le contenu de `mysql-data/` est gere par le moteur SQL local et n'est pas versionne.
